package core.game.system.timer

/**
 * Timer flag
 *
 * @constructor Timer flag
 */
enum class TimerFlag {
    /**
     * Clear on death
     *
     * @constructor Clear on death
     */
    ClearOnDeath
}