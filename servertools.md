https://gitlab.com/rs-source/server-tools
___

**Setup**
* Set up `db` user with read/write permissions on server database
* Put creds in `credentials.json`
* `sudo apt install libmariadb3 libmariadb-dev`
